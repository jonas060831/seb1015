const initialRoutes = require('./initialRoutes.controller')
const postRoutes = require('./postRoutes.controller')
const commentRoutes = require('./commentRoutes.controller')
const contributorsRoutes = require('./contributorsRoutes.controller')

module.exports = {
    initialRoutes,
    postRoutes,
    commentRoutes,
    contributorsRoutes
}